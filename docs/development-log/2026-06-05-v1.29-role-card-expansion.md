# v1.29 角色卡擴充與地標補卡

日期：2026-06-05
程式上架 commit：`46ed70b Expand red epic and landmark role cards`
GitHub Pages run：`26988794603`，部署成功

## 本次變更

- APP_VERSION 從 `v1.28` 升到 `v1.29`。
- 新增 `src/profiles/changxiangsiRedEpicProfiles.js`，加入 30 張長相思紅衣封神旅拍角色卡。
- `src/profiles/landmarkProfiles.js` 追加 30 張世界頂級網紅地標旅拍角色卡。
- `iconic-checkin-*` 角色卡改為信任明確 `parentCategory`，避免被「大唐」「白裘」等泛用關鍵字誤判。
- `tests/promptEngine.test.js` 新增長相思紅衣封神測試，並把地標測試更新為 77 張。
- `scripts/prepare-vite-entry.mjs` 與 `scripts/verify-ui.mjs` 同步版本為 `v1.29`。

## 新增資料量

- `WORLD_LAYER_PROFILES`：1,893
- `iconic-checkin-*`：77
- `changxiangsi-red-epic-*`：30
- duplicate profile id：0

## 上架前檢查

已執行 `npm run check`：

- `sync:spec`：通過，`src/coreSpec.js` 同步 6,179 chars
- `lint`：0 errors
- `test`：58/58 passed
- `build`：成功，standalone `index.html` 628.86 KB
- `verify:ui`：desktop/mobile 通過，無水平 overflow，consoleErrors 0

保留事項：Vite 仍有單一 JS chunk 大於 800 KB 的既有警告。本次只做角色卡資料擴充與版本上架，未混入 chunk splitting。
