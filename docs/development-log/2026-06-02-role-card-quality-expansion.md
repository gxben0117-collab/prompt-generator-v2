# 2026-06-02 全角色卡品質升級與分類擴充紀錄

## 目標

依使用者要求，完成以下事項：

- 讀取並延續 `AI-TASK.md` 的後續建議。
- 對全角色卡做品質升級，不只手修少量卡片。
- 盤點全分類角色卡缺口，針對不足分類建立新角色卡。
- 重新執行全專案檢查、更新流程文件並上架。

## 處理策略

本次沒有重寫固定母板，也沒有短版化核心咒語結構。改動集中在角色卡資料層、分類路由與測試守門。

採用兩層處理：

1. 全庫品質補強：
   - 在 `src/data.js` 的 `enrichSceneAction()` 增加「全角色卡品質補強」。
   - 所有 `WORLD_LAYER_PROFILES` 匯出前都會補上可拍攝動作、支撐點、臉部不可遮擋、真人骨架與受力合理要求。
   - 保留原本每張卡的動作內容，只追加品質守門，不覆蓋角色身份。

2. 分類缺口擴充：
   - 新增 `src/eighthWaveProfiles.js`。
   - 補 24 張新角色卡，避免繼續塞大 `src/data.js` 主檔。
   - 針對盤點後較少的 8 個父分類，各補 3 張。

## 新增分類卡

第八波共 24 張，父分類分布：

- 東方和風旅拍：3
- 東方旗袍夜宴：3
- 賽博機甲 / 科幻戰姬：3
- 現代都市 / 街拍電影：3
- 世界地標旅拍：3
- 室內生活寫真：3
- 海岸度假旅拍：3
- 高訂婚紗禮服：3

代表卡：

- 京都鳥居雨巷・朱傘和服旅人
- 上海爵士夜・墨綠旗袍歌姬
- 銀灰機庫・女指揮官
- 首爾黑膠街角・皮衣音樂人
- 倫敦塔橋晨霧・風衣旅拍女主
- Loft 窗邊晨光・針織生活寫真
- 馬爾地夫水屋・白裙海風旅人
- 巴黎高訂工坊・象牙婚紗設計師

## 分類與資料修正

- 移除 2 個 ghost `ROLE_CATEGORIES`：
  - `長相思旅拍／西安古城紅衣電影`
  - `民族古城旅拍／漢服民族風／夜景電影`
- `parentCategoryForProfile()` 增加對 `eighth-*` 角色卡的 `parentCategory` 尊重，避免明確分類被都市夜景等 priority keyword 吃掉。

## 新增守門測試

`tests/promptEngine.test.js` 新增/強化：

- 第八波 24 張卡必須存在。
- 第八波 8 個父分類必須各 3 張。
- 所有 `WORLD_LAYER_PROFILES` 都必須包含 `全角色卡品質補強`。
- 不允許保留沒有角色卡的 ghost categories。

## 驗證流程

標準全專案檢查：

```powershell
npm.cmd run check
```

內容包含：

- `sync:spec`
- `lint`
- `test`
- `build`
- `verify:ui`

已知提醒：

- Vite large chunk warning 仍屬目前單頁資料量大的正常提醒，不阻斷輸出。

## 後續建議

- 下一波新增角色卡請繼續使用新 wave 檔，不要再大幅增加 `src/data.js` 主檔。
- 若使用者實測仍有高比例站姿，優先調整 `inferActionCueTexts()` 與 `actionQualityGuardText()`，比逐張手修更有效。
- 若資料量再明顯增加，再評估 code splitting；目前仍以單頁穩定上架為優先。
